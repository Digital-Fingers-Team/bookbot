# AradoBot Obsidian Vault

Open this folder (`docs/vault`) as a vault in Obsidian. Start at [[Home]].

Kept deliberately small so it can be read in full at the start of a session:
`Home` → `Repo Map` → the area note you need. `Open Threads` holds anything still unresolved — update it as things close.
